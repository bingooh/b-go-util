package util

/*func TestStream(t *testing.T) {
	r := require.New(t)

	rs1 := []int{1, 2, 3}
	stream1 := _stream.OfSlice(0, rs1...)
	rs11 := _stream.Drain(stream1)
	r.EqualValues(rs1, rs11)
	_, ok1 := <-stream1
	r.False(ok1)

	rs2 := []string{`1`, `2`}
	stream2 := _stream.OfSlice(0, `1`, `2`)
	rs22 := _stream.Drain(stream2)
	r.EqualValues(rs2, rs22)
	_, ok2 := <-stream2
	r.False(ok2)
}*/
